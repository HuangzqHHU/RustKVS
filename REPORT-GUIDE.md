# 课程报告模板与写作指南

> 依据：《高级系统编程技术课程要求》PPT 第 8 页（课程报告 6 项要求）+ 毕设论文规范
> 要求：**每人一份**，参考毕设论文规范撰写，打印装订，凭报告参加答辩
> 建议篇幅：正文 3000-5000 字，图表代码不计
> 项目事实数据（全组通用，可引用）：179 个自动化测试、8 个源模块、4 天开发、零第三方依赖

---

## 封面

- 题目：**基于 Rust 的可持久化网络键值存储系统的设计与实现**
- 姓名 / 学号 / 班级 / 组别（四人小组）
- 承担角色（A组长/B存储持久化/C解析客户端/Web页面/D测试）

---

## 摘要（毕设规范，150-250 字）

> 本文设计并实现了一个基于 Rust 的……系统。系统由服务器、命令行客户端和 Web 管理界面组成，实现了键值增删改查、TCP 多客户端并发、追加日志持久化与启动恢复，并扩展了 TTL 过期时间、实时状态统计等功能。全部代码基于 Rust 标准库实现（零第三方依赖），配套 179 个自动化测试。

## 关键词

Rust；键值存储；TCP 网络编程；并发；持久化；所有权

---

## 一、课程设计任务

- **1.1 系统总体目标**：一句话目标（可持久化、多客户端访问的键值存储系统），服务器+客户端组成
- **1.2 功能边界**：数据操作（增查改删）、并发访问、数据持久化；明确不做：分布式、ACID 事务等
- **1.3 本人承担内容**（★按自己角色写，引用真实工作）：
  - 写清楚：负责的模块、写的函数、解决的问题
  - A（组长）示例见文末"附录一"

## 二、系统方案设计及创新性设计

- **2.1 总体架构与模块划分**：引用 `DESIGN.md` 结构图；protocol/parser/store/persistence/server/client/web/webpage 各模块职责一句话
- **2.2 协议设计**：一行一条消息、`\n` 边界、命令表、错误码、MAX_MSG_LEN（引用 `PROTOCOL.md`）
- **2.3 持久化设计**：追加式日志、先写日志再更新内存、启动重放、损坏文件明确报错（讲清"为什么先写日志"）
- **2.4 并发安全设计**（重点）：每连接一线程 + `Arc<Mutex<Server>>` + **锁最小化**（只在 execute 期间持锁）；原子计数（连接数/命令数）
- **2.5 创新性设计**（扩展分关键）：
  - TTL 过期：存储 `(value, Option<Instant>)`，惰性过期检查
  - Web 管理：纯 std 手写 HTTP（GET+POST 表单），与 TCP 共享 Server
  - STATUS 增强：connections/uptime/commands 实时统计
  - execute 三层：execute(计数) → execute_inner(核心) → execute_without_count(页面渲染)
- **2.6 可扩展性与可维护性**：模块接口化（KVStore 独立可复用）、零依赖便于构建、文档齐全

## 三、方案论证及可行性分析

逐项论证（各 3-5 行）：
- **协议方案**：为什么用换行定界而非长度前缀——实现简单、命令文本自然；MAX_MSG_LEN 防超长
- **存储方案**：HashMap 满足功能要求，O(1) 读写；TTL 惰性删除避免定时器复杂度
- **持久化方案**：追加日志比定期快照简单可靠；"先日志后内存"保证已确认数据不丢（对照 PPT 要求）
- **并发方案**：线程模型满足"多客户端"要求；Mutex 保证安全；锁范围最小化保证吞吐
- **测试方案**：单元+集成+网络+并发+压力 179 个用例分层覆盖

## 四、设计过程及设计成果

- **4.1 团队分工**：四人模块划分表（见 `GIT协作指南.md`）
- **4.2 进度安排**：4 天：D1 框架+协议契约 → D2 内存存储+解析 → D3 网络+持久化 → D4 并发+扩展；每天 16:30 集成节点、程序始终可运行
- **4.3 程序流程**：贴一张关键代码/流程图（如 execute 流程：解析→写日志→更新内存→响应）
- **4.4 调试过程**（★有故事可写，真实经历）：
  - 接口变更导致编译失败两次（parser 改名、execute 改方法）→ 教训：契约先行
  - 页面渲染污染 commands 计数 → 解决方案（只读快照/execute 三层）
  - 浏览器缓存导致 TTL 不刷新 → Cache-Control 头
  - Web 手写 HTTP 的踩坑（Content-Length 中文、%XX 解码）
- **4.5 最终演示效果**：对照 DEMO.md 八场景，描述演示结果（多客户端并发、重启恢复、TTL 可视化、Web 管理）

## 五、工程管理与质量控制

- **5.1 任务分解**：EXCELLENCE-PLAN / WEB-PLAN 的接口契约先行策略
- **5.2 测试覆盖**：179 测试分布表（lib 68/集成 46/网络 20/持久化 19/并发 8/压力 13/Web 4）
- **5.3 代码规范**：`cargo clippy`、注释规范、模块职责单一
- **5.4 集成过程**：Git 分支协作、每天 16:30 集成节点、tag 标记、冲突解决记录（以 C 方案为准的案例）
- **5.5 进度控制**：接口变更登记（parser→parse_command、execute→Server 方法→三层）、合并后立即 cargo test

## 六、课程设计收获及体会

- **6.1 Rust 学习**：所有权/借用、Result 错误处理、trait、模块组织；编译期就发现错误
- **6.2 系统设计**：接口先行、模块解耦、分层设计（传输/协议/存储分离）
- **6.3 团队协作**：Git 协作规范、接口变更同步、冲突处理、互相 review
- **6.4 问题解决**：30 分钟求助规则、用测试定位回归、curl 排除浏览器缓存等排查方法论

---

## 附录一：成员A（组长）"本人承担内容"参考写法

> 本人在小组中担任组长与架构师，承担工程搭建、协议契约定稿、服务器实现与集成协调工作，具体包括：
> 1. **工程与协作**：创建 Cargo 工程与模块划分（8 个模块）、制定 Git 协作规范与每日集成节点制度，负责 4 天开发中的代码合并与冲突解决；
> 2. **协议契约**：定稿命令表（SET/GET/DEL/LIST/STATUS/PING/EXIT）、消息格式、错误码与长度限制（`PROTOCOL.md`、`protocol` 模块）；
> 3. **服务器**：实现主循环、TCP 网络化（`TcpListener` + `BufReader` 处理消息分段）、持久化接入（先写日志再更新内存、启动恢复）、多客户端并发（`thread::spawn` + `Arc<Mutex<Server>>` + 锁最小化）、启动参数化（`--port/--data/--web-port`）、STATUS 增强统计；
> 4. **Web 管理**：实现零依赖手写 HTTP 协议层（`web.rs`：请求解析、URL 解码、表单解析、HTML 转义、响应组装）；
> 5. **质量与文档**：撰写 `README/DEMO/DESIGN/验收记录`，组织 179 个测试全绿与演示彩排。

## 附录二：各组员素材速查（写"本人承担内容"和调试经历时用）

| 成员 | 真实工作素材 |
|---|---|
| A（组长） | 见附录一；另含：接口变更协调、冲突解决（以 C 方案为准）、STATUS/计数 bug 修复 |
| B | `store`（HashMap+TTL 惰性过期）、`persistence`（append 自动建目录、recover 带行号报错）、store 单元测试；调试经历：TTL 数据结构改造 |
| C | `parser`（7 命令+TLL 解析+take_word 手写分词）、`client`（本地 REPL/TCP REPL/超时）、`webpage`（数据表/命令表单/GET /data）、execute 三层方案、多参数/超长校验测试 |
| D | 测试规划（四天清单）、integration 46/network 20/persistence 19/stress 14/concurrency 8/web_verify 4、网络测试服务器构造（临时端口+独立数据文件） |

## 附录三：格式要求（参照毕设论文）

- 页边距 2.5cm，宋体小四正文，1.5 倍行距
- 标题分级编号：一、1.1、1.1.1
- 图/表编号（图 1、表 1）并引用
- 代码用等宽字体、短代码段
- 参考文献 3-5 条（Rust 官方文档、The Rust Programming Language、RUNOOB 教程等）
- 打印装订，页脚页码
