# 答辩演示脚本（DEMO）

> 演示人：成员A（组长） | 配合：组员
> 目标：10 分钟流畅完成 PPT 验收要求 + 扩展功能展示
> 原则：先跑通一遍再上场；每步讲"发生了什么 + 为什么"

## 演示前准备（5 分钟）

```bash
cargo build
cargo test            # 确认 179 个测试全绿（可展示）
# 清理旧数据（保证从空库开始）
Remove-Item data/kv.log -ErrorAction SilentlyContinue
```

## 场景一：启动服务器（1 分钟）

**操作**：终端 1 运行 `cargo run -- server`

**预期输出**：
```
kvstore 服务器已启动（第4天：网络模式，多客户端并发）
监听地址: 127.0.0.1:7878
数据文件: data/kv.log
启动状态: 正在监听，等待客户端连接...
Web 管理: http://127.0.0.1:8080
```

**讲解词**："服务器监听 7878 命令端口和 8080 Web 端口，数据持久化到 data/kv.log。注意我们零第三方依赖，HTTP 都是手写的。"

## 场景二：基本增删改查（2 分钟）—— PPT 验收

**操作**：终端 2 运行 `cargo run -- client`，依次输入：

| 输入 | 预期 | 讲解 |
|---|---|---|
| `SET course Rust` | OK | 写入 |
| `GET course` | VALUE course Rust | 查询 |
| `SET course Rust进阶` | OK | 覆盖写 |
| `GET course` | VALUE course Rust进阶 | 查到新值 |
| `GET notexist` | ERROR 键不存在 | 不存在提示 |
| `LIST` | KEYS course | 列键 |
| `STATUS` | STATUS count=1 connections=1 uptime=..s commands=7 | **状态增强** |

**讲解词**："命令大小写不敏感、错误有中文提示。注意 STATUS 显示连接数、运行时长、命令总数——这是我们增强的实时统计。"

## 场景三：错误处理与容错（1 分钟）—— PPT 验收

**操作**：继续输入：

| 输入 | 预期 |
|---|---|
| `FOO x` | ERROR 未知命令：FOO |
| `SET onlykey` | ERROR 缺少参数：SET 需要 value |
| `GET course extra` | ERROR 多余参数 |

**讲解词**："每条错误后客户端都不崩溃、能继续输入——单条命令出错不影响会话。直接关闭这个客户端窗口，服务器也不受影响。"

## 场景四：多客户端并发（1.5 分钟）—— PPT 验收

**操作**：再开两个终端运行 `cargo run -- client`（共 3 个客户端），同时操作：

- 客户端 A：`SET name Alice`
- 客户端 B：`SET book Rust`
- 客户端 C：`GET name` → 能查到 A 写的（**共享数据**）
- 任意客户端：`STATUS` → count 增长

**讲解词**："每个连接一个线程，共享同一份存储（Arc+Mutex）。关键设计是**锁最小化**——只在执行数据操作的瞬间加锁，等待网络输入时不持锁，所以 3 个客户端同时操作互不阻塞。"

## 场景五：重启恢复（1.5 分钟）—— PPT 核心验收

**操作**：
1. 终端 1 服务器按 `Ctrl+C` 停止
2. 重新 `cargo run -- server`
3. 客户端：`GET course` → **VALUE course Rust进阶**（数据还在！）

**讲解词**："这是核心：每次写操作**先写日志文件、成功后才更新内存、再返回 OK**。所以客户端收到成功时数据已在磁盘。重启时日志逐行重放恢复。展示一下日志文件内容：`Get-Content data/kv.log`。"

## 场景六：TTL 过期时间（1.5 分钟）—— 创新扩展①

**操作**（客户端）：
```
SET temp hello 5     → OK
GET temp             → VALUE temp hello（立即能查到）
```
**等待 6 秒**，再：
```
GET temp             → ERROR 键不存在（已过期！）
```

**讲解词**："TTL 是创新扩展。存储时记录过期时刻，读取时惰性检查——过期键自动视为不存在。演示效果：写入 5 秒后键自动消失。"

## 场景七：Web 管理界面（2 分钟）—— 创新扩展②

**操作**：打开浏览器 `http://127.0.0.1:8080`

1. **先展示数据**：页面表格显示 course= Rust进阶（与客户端数据一致——共享）
2. **网页执行命令**：输入框输 `SET webkey webvalue 6` 点执行 → OK，表格出现
3. **TTL 可视化**：等 6 秒后**手动 F5 刷新**（或切回终端观察）→ webkey 从表格消失
4. **反向互通**：切回客户端 `GET webkey` → 键不存在（网页删除对客户端生效）

**讲解词**："Web 管理是第二个扩展，纯 std 手写 HTTP。网页与命令行客户端共享同一份数据、同一套持久化逻辑——你在网页删的键，命令行也查不到。这个功能展示数据可视化和系统完整性。"

## 场景八：测试与稳定性展示（1 分钟，可选）

**操作**：终端运行 `cargo test`，展示全绿输出

**讲解词**："179 个自动化测试，覆盖：命令解析、数据操作、持久化恢复、网络通信、多客户端并发、压力测试（100 并发）和 Web 验证。"

---

## 常见提问准备（答辩）

| 问题 | 回答要点 |
|---|---|
| 为什么用线程不用异步？ | 每连接一线程 + 锁最小化已满足课程规模；异步引入 tokio 依赖且复杂度高 |
| 锁为什么最小化？ | 避免慢客户端阻塞所有人；只保护数据操作瞬间 |
| 为什么先写日志再更新内存？ | 保证客户端收到 OK 时数据已落盘，断电不丢；写日志失败返回错误不返回虚假成功 |
| 文件损坏怎么办？ | recover 时报明确错误退出，绝不静默清空（有测试覆盖） |
| TTL 重启后？ | 日志格式不记 ttl，重启后转为永久（设计取舍，可扩展） |
| 命令计数怎么实现的？ | execute 三层：execute(计数)→execute_inner(核心)→execute_without_count(页面渲染不计数) |
| 多客户端同时写同一键？ | Mutex 串行化，最终值是某一次写入，不损坏（有测试） |
