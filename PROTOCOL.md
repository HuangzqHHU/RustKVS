# 协议契约 v1（第1天定稿）

> 全组必须遵守的通信规则。本文件是唯一权威；任何变更须在站会/集成节点同步。
> 对应代码：`src/protocol/mod.rs`

## 1. 命令表

| 命令 | 参数 | 含义 | 成功响应 | 失败响应 |
|---|---|---|---|---|
| `SET` | `key value` | 写入或覆盖键值 | `OK` | `ERROR <说明>` |
| `GET` | `key` | 查询键值 | `VALUE <key> <value>` | `ERROR 键不存在` |
| `DEL` | `key` | 删除键值 | `OK` | `ERROR 键不存在` |
| `LIST` | 无 | 列出全部键 | `KEYS k1 k2 k3`（可空） | — |
| `STATUS` | 无 | 查看数据数量与状态 | `STATUS count=<n>` | — |
| `PING` | 无 | 检查连接 | `PONG` | — |
| `EXIT` | 无 | 客户端退出并关闭连接 | `BYE` | — |

## 2. 消息格式

- 一行一条请求/响应，字段以**空格**分隔，以**换行符 `\n`** 结尾。
- 请求：`<COMMAND> [参数...]`，命令名大小写不敏感，规范输出用大写。
- 响应：`OK [信息]` / `ERROR <说明>` / 数据行（`VALUE`、`KEYS`、`STATUS`、`PONG`、`BYE`）。
- **键**不允许包含空格或换行；**值**不允许换行（值中的空格允许：`SET` 取第二个空格之后整体为值）。
- 单条消息最大长度：**1024 字节**（`MAX_MSG_LEN`），超过返回 `ERROR 消息超长`。

## 3. 错误码（错误提示统一为中文，清晰可读）

| 错误 | 说明 |
|---|---|
| 未知命令 | 无法识别的命令名 |
| 缺少参数 | 参数个数少于命令要求 |
| 多余参数 | 参数个数多于命令要求 |
| 键不存在 | GET/DEL 的键不存在 |
| 非法键 | 空键、含空格或换行的键 |
| 消息超长 | 单条消息超过 1024 字节 |

## 4. 容错要求

- **单条命令出错不影响连接**：返回 `ERROR` 后，连接继续处理下一条合法命令。
- 客户端主动断开、发送非法文本、消息分段，服务器不得崩溃。
- 服务器返回成功前，数据必须已可靠写入（见持久化方案）。

## 5. 默认配置

| 项 | 值 | 说明 |
|---|---|---|
| 监听地址 | `127.0.0.1:7878` | `DEFAULT_ADDR` |
| 数据文件 | `data/kv.log` | `DEFAULT_DATA_FILE` |
